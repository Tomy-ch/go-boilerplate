<!-- markdownlint-disable MD041 -->
## 更新概要

- ドキュメントの整備とラベル設定の更新を実施。

## 更新内容

- `.github/settings/labels.json` に以下の変更を加えました:
  - `triage` ラベルの色を `d93f0b` に変更。
  - `security` ラベルの色を `5fafe6` に変更。
  - 新しいラベル `release` を追加。
- `internal/` 以下の各ディレクトリに README.md を追加し、役割や注意点を明記。
- `openapi/` 以下のドキュメントを更新し、設計ポリシーやパラメータ定義のガイドラインを明確化。

## 追加ライブラリ

- なし

## 不具合修正

- なし
