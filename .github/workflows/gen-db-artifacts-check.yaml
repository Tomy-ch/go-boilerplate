name: Generated Database Artifacts Check

# `paths` lists the generated output as well as the inputs — see README.md for why.
on:
  pull_request:
    paths:
      # Inputs
      - '.github/workflows/gen-db-artifacts-check.yaml'
      - '.github/actions/setup-postgres/**'
      - 'database/**/*.sql'
      - 'docker/database/**/*.sql'
      - '.makefiles/**/*.mk'
      - 'sqlc.yaml'
      - 'mise.toml'
      # `dump-schema` and `merge-dml` are what actually write `database/gen/**`, so their
      # implementation is an input here. Only these two packages are listed — the rest of the
      # module is not a generator and watching it would amount to dropping the filter.
      - 'internal/cli/dumpschema/**'
      - 'internal/cli/mergedml/**'
      - 'cmd/dump_schema.go'
      - 'cmd/merge_dml.go'
      # Generated output. `database/gen/**` is already covered by `database/**/*.sql` above.
      - 'internal/infrastructure/rdb/sqlc/gen/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate-db-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write
    env:
      ENV: ci
      DB: test
      DB_USER: postgres
      DB_PASSWORD: postgres-password

    services:
      database:
        image: postgres:18.4-trixie
        env:
          POSTGRES_USER: ${{ env.DB_USER }}
          POSTGRES_PASSWORD: ${{ env.DB_PASSWORD }}
          POSTGRES_DB: ${{ env.DB }}
          POSTGRES_INITDB_ARGS: "--encoding=UTF8"
          TZ: Asia/Tokyo
          PGTZ: Asia/Tokyo
        ports:
          - "5432:5432"
        options: >-
          --health-cmd="pg_isready -U postgres -d test"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
          --health-start-period=20s

    steps:
      - name: Harden the runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: block
          allowed-endpoints: >
            agent.api.stepsecurity.io:443
            api.github.com:443
            github.com:443
            codeload.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            *.actions.githubusercontent.com:443
            *.blob.core.windows.net:443
            proxy.golang.org:443
            sum.golang.org:443
            index.golang.org:443
            storage.googleapis.com:443
            dl.google.com:443
            go.dev:443
            golang.org:443
            mise.jdx.dev:443
            mise-versions.jdx.dev:443
            aquaproj.github.io:443
            docker.io:443
            index.docker.io:443
            registry-1.docker.io:443
            auth.docker.io:443
            production.cloudflare.docker.com:443
            production.cloudfront.docker.com:443
            dl-cdn.alpinelinux.org:443
            dl-cdn.alpinelinux.org:80
            mise.run:443
            deb.debian.org:443
            deb.debian.org:80
            security.debian.org:443
            security.debian.org:80
            pypi.org:443
            files.pythonhosted.org:443
            *.docker.io:443
            mirror.gcr.io:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
            azure.archive.ubuntu.com:80
            archive.ubuntu.com:80
            security.ubuntu.com:80
            esm.ubuntu.com:443
            motd.ubuntu.com:443
            apt.postgresql.org:80
            apt.postgresql.org:443
            www.postgresql.org:443
            tuf-repo-cdn.sigstore.dev:443
            rekor.sigstore.dev:443
            fulcio.sigstore.dev:443
            nodejs.org:443
            registry.npmjs.org:443
            downloads.sqlc.dev:443
            astral.sh:443

      # fetch-depth: 0 so the PR's base and head commits are present locally. The default
      # shallow checkout fetches only the merge commit, so the `git diff` that classifies the
      # cause below cannot resolve either parent.
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup mise
        uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          version: 2026.6.0
          install: false
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install sqlc
        run: mise install sqlc
      - name: Setup DB
        uses: ./.github/actions/setup-postgres
        with:
          db: ${{ env.DB }}
          user: ${{ env.DB_USER }}
          password: ${{ env.DB_PASSWORD }}
          init_sql: docker/database/sql/004-init-extensions-test-db.sql
      - name: Materialize ci env for embedding
        run: make materialize-env
      - name: Initialize Database Table
        run: make db-migrate-ci-up DB="$DB"
      - name: Dump schema
        run: make dump-schema-ci work-dir="."
      - name: Merge DML
        run: make merge-dml-ci work-dir="."
      - name: Remove generated SQLC code
        run: make remove-generated-sqlc-ci
      - name: Generate SQLC code
        run: make sqlc-generate-ci
      - name: Format generated code
        run: make fmt
      # The materialized env/.env is not generated output, so exclude it from the drift check.
      - name: Restore embedded env
        run: make restore-env
      - name: Detect generated changes (file list)
        id: gen_diff
        shell: bash
        run: |
          set -euo pipefail
          git add -A
          files="$(git diff --cached --name-only)"
          if [ -n "$files" ]; then
            echo "has_diff=true" >> "$GITHUB_OUTPUT"
            printf "%s\n" "$files" > /tmp/changed_files.txt
          else
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
            : > /tmp/changed_files.txt
          fi
      # Classify the PR so the report can name the actual cause:
      #   - source_modified:   the SQL source changed, so a forgotten `make gen-query` is likely
      #   - artifact_modified: the generated output was edited directly, which is a rule violation
      #   - neither:           generator-side (sqlc) drift is the likely cause
      # `database/gen/**` is generated output, so it is excluded from the source set even though it
      # is SQL under `database/`.
      - name: Detect source modifications in this PR
        id: source_mod
        if: github.event_name == 'pull_request' && steps.gen_diff.outputs.has_diff == 'true'
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          base_sha="${BASE_SHA-}"
          head_sha="${HEAD_SHA-}"
          # Surface a diff failure rather than degrading to an empty file list, which would
          # silently misreport every cause below as generator-side drift.
          if ! changed="$(git diff --name-only "$base_sha" "$head_sha")"; then
            echo "::warning::failed to diff ${base_sha}..${head_sha}; cause classification skipped"
            changed=""
          fi
          if echo "$changed" | grep -E '^(database|docker/database)/.*\.sql$' | grep -qvE '^database/gen/'; then
            echo "source_modified=true" >> "$GITHUB_OUTPUT"
          else
            echo "source_modified=false" >> "$GITHUB_OUTPUT"
          fi
          if echo "$changed" | grep -qE '^(database/gen/|internal/infrastructure/rdb/sqlc/gen/)'; then
            echo "artifact_modified=true" >> "$GITHUB_OUTPUT"
          else
            echo "artifact_modified=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Build PR comment body
        id: comment
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          HAS_DIFF: ${{ steps.gen_diff.outputs.has_diff }}
          SOURCE_MODIFIED: ${{ steps.source_mod.outputs.source_modified }}
          ARTIFACT_MODIFIED: ${{ steps.source_mod.outputs.artifact_modified }}
        run: |
          set -euo pipefail
          out=/tmp/gen-db-comment.md
          if [ "${HAS_DIFF-}" != "true" ]; then
            echo "title=## ✅ No differences in database output were detected" >> "$GITHUB_OUTPUT"
            echo "No differences in database output were found." > "$out"
            exit 0
          fi
          # Disable SC2016: `make ...` in the notification message is shown literally (must not expand)
          # shellcheck disable=SC2016
          if [ "${SOURCE_MODIFIED-}" = "true" ]; then
            echo "title=## 🧾 Differences in database output have been detected" >> "$GITHUB_OUTPUT"
            printf '%s\n' "Run the database generation command (e.g. \`make gen\` / \`make gen-query\`) locally and commit the changes." > "$out"
          elif [ "${ARTIFACT_MODIFIED-}" = "true" ]; then
            echo "title=## ✋ DB generated output was edited directly" >> "$GITHUB_OUTPUT"
            printf '%s\n' "This PR changes the DB generated output without changing any SQL source. Generated files must not be hand-edited (see \`AGENTS.md\`). Discard the edit, run \`make gen-query\` so the output comes from the generator, and commit that instead." > "$out"
          else
            echo "title=## ⚠️ Drift in DB generated output detected (SQL source unchanged)" >> "$GITHUB_OUTPUT"
            printf '%s\n' "This PR does not modify any SQL source, so this may be generator-side drift (a change to \`sqlc.yaml\` / \`mise.toml\` / the DB setup action, sqlc version differences, stale generated artifacts on the base, etc.). Run \`make gen-query\` locally and commit, or update the base via \`auto-generate-docs\` and then rebase / merge." > "$out"
          fi
          # Same computation as fenceFor in .github/actions/upsert-pr-comment/action.yaml, repeated
          # because that helper only wraps a body on the details-summary path and this call passes
          # the body through untouched.
          # `-a`: without it a single NUL byte makes grep report a binary match and print nothing,
          # collapsing the fence back to three backticks.
          fence_for() {
            local longest len
            longest="$({ grep -a -o '`\{1,\}' "$1" || true; } | awk '{ if (length($0) > n) n = length($0) } END { print n + 0 }')"
            len=$(( longest + 1 ))
            if [ "$len" -lt 3 ]; then len=3; fi
            printf '%*s' "$len" '' | tr ' ' '\140'
          }

          # A path may hold any byte but NUL and `/`, so a name carrying a backtick closes an inline
          # code span early and renders the rest as live Markdown under the bot's name — a mention
          # notifying third parties, a fabricated heading or link. Fencing the list instead, at a
          # length taken from the list itself, leaves no run able to close it.
          list=/tmp/gen-db-filelist.txt
          # The action's length cap slices a body without regard for fences and does not re-fence on
          # this path, so a list trimmed there would lose the closing fence. Trimming here, before
          # the fence is sized, keeps the block closed and stays under the 45000 character default.
          # The fence cannot outgrow that budget either: a backtick run never spans a `/`, so the
          # longest one a path can hold is bounded by NAME_MAX — 255 bytes — and the fence by 256.
          list_limit=40000
          head -c "$list_limit" /tmp/changed_files.txt > "$list"
          if [ "$(wc -c < /tmp/changed_files.txt)" -gt "$list_limit" ]; then
            printf '\n... (file list truncated)\n' >> "$list"
          fi
          fence="$(fence_for "$list")"
          {
            echo ""
            echo "${fence}text"
            cat "$list"
            echo "${fence}"
          } >> "$out"

      - name: Comment generated artifacts result on PR
        if: always() && github.event_name == 'pull_request'
        continue-on-error: true
        uses: ./.github/actions/upsert-pr-comment
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          marker: '<!-- generated-artifacts-filelist -->'
          status: "${{ steps.gen_diff.outputs.has_diff == 'false' && 'success' || 'drift' }}"
          title: "${{ steps.comment.outputs.title || '## ⚠️ Generated Database Artifacts Check: CUT OFF (no result produced)' }}"
          body-file: /tmp/gen-db-comment.md
      - name: Fail if diff exists
        if: steps.gen_diff.outputs.has_diff == 'true'
        run: exit 1
