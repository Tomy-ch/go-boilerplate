get:
  operationId: GetHealthz
  tags:
    - healthz
  summary: サーバーのヘルスチェック
  description: サーバーが正常に稼働しているかを確認するためのEKS向けのヘルスチェックエンドポイントです。
  security: []
  responses:
    '200':
      description: サーバーは正常に稼働しています。
      content:
        application/json:
          schema:
            $ref: '../components/responses/health-check/HealthResponse.yaml'
    '405':
      $ref: '../components/schemas/errors/MethodNotAllowed405.yaml'
