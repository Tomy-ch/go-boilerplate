get:
  operationId: GetJwks
  tags:
    - discovery
  summary: JWK Set の取得
  security: []
  responses:
    '200':
      description: 署名検証用の公開鍵一覧
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/Jwks.yaml'
