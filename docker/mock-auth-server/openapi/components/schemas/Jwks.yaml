type: object
description: >
  JSON Web Key Set（RFC 7517）。公開鍵のみを含む。Go 側の署名検証が依存する契約であり、
  鍵（kid=mock-key-1 / RS256）はバイト等価で不変に保つ。
required:
  - keys
properties:
  keys:
    type: array
    description: 署名検証用の公開鍵一覧。
    items:
      type: object
      required:
        - kty
        - kid
        - alg
        - "n"
        - e
      properties:
        kty:
          type: string
          example: RSA
        kid:
          type: string
          example: mock-key-1
        use:
          type: string
          example: sig
        alg:
          type: string
          example: RS256
        "n":
          type: string
          description: RSA modulus（base64url）。
        e:
          type: string
          description: RSA exponent（base64url）。
          example: AQAB
