// Package gen provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.7.0 DO NOT EDIT.
package gen

import (
	"time"

	openapi_types "github.com/oapi-codegen/runtime/types"
)

const (
	BearerAuthScopes bearerAuthContextKey = "BearerAuth.Scopes"
)

// ErrorResponse エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ErrorResponse struct {
	// Code 機械的に処理可能なエラーコード
	Code string `json:"code"`

	// Message 人間が読めるエラーメッセージ
	Message string `json:"message"`

	// RequestId リクエストID
	RequestId string `json:"requestId"`
}

// PurchaseDispatchGroupResponse まとめて発送してよい購入の組。同一の購入者宛てであることを軸に分けます。
// 組は算出結果であり永続化しないため、組そのものの識別子は持ちません。
type PurchaseDispatchGroupResponse struct {
	// Purchases 組に含まれる購入。注文日時の古い順（同時刻は購入 ID の昇順）で並びます。
	Purchases []PurchaseShippableItemResponse `json:"purchases"`

	// UserId 組に含まれる購入の購入者 ID。組の中では全件が同一です。
	UserId openapi_types.UUID `json:"userId"`
}

// PurchaseShippableItemResponse まとめ発送の組に含まれる購入 1 件のスキーマ。発送指示は購入 1 件ずつ code を指定して行います。
// 組に入っている時点で発送可能なので、status は返しません。明細も含みません（購入詳細 API で取得します）。
type PurchaseShippableItemResponse struct {
	// Code 購入コード（利用者へ注文番号として見せる一意の識別子）。発送指示（`PATCH /v1/purchases/{purchaseCode}/ship`）に用います。
	Code string `json:"code"`

	// OrderedAt 注文日時
	OrderedAt time.Time `json:"orderedAt"`

	// TotalAmount 合計（小計 + 税額 + 送料）。USD セント単位の整数です。
	TotalAmount int64 `json:"totalAmount"`
}

// PurchaseShippableResponse 発送待ち購入のまとめ発送一覧のレスポンススキーマ。まとめて発送してよい組の配列を返します。
// 組同士はその組の最も古い購入の注文日時の順（同時刻は購入 ID の昇順）で並びます。
type PurchaseShippableResponse struct {
	// Groups まとめ発送の組の一覧。発送待ちの購入がない場合は空配列です。
	Groups []PurchaseDispatchGroupResponse `json:"groups"`
}

// ShippableLimitParam defines model for ShippableLimitParam.
type ShippableLimitParam = int

// BadRequest400 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type BadRequest400 = ErrorResponse

// Forbidden403 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Forbidden403 = ErrorResponse

// InternalServerError500 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type InternalServerError500 = ErrorResponse

// MethodNotAllowed405 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type MethodNotAllowed405 = ErrorResponse

// ServiceUnavailable503 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ServiceUnavailable503 = ErrorResponse

// Unauthorized401 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Unauthorized401 = ErrorResponse

// basicAuthContextKey is the context key for BasicAuth security scheme
type basicAuthContextKey string

// bearerAuthContextKey is the context key for BearerAuth security scheme
type bearerAuthContextKey string

// GetPurchasesShippableParams defines parameters for GetPurchasesShippable.
type GetPurchasesShippableParams struct {
	// Limit 読み出す発送待ち購入の件数。注文日時の古い順で上位 limit 件を読み、まとめ判定はその範囲の中で行います。
	// 範囲の外にある同一購入者の購入は別の便になります。
	Limit *ShippableLimitParam `form:"limit,omitempty" json:"limit,omitempty"`
}
