// Package gen provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.7.0 DO NOT EDIT.
package gen

import (
	"time"

	openapi_types "github.com/oapi-codegen/runtime/types"
)

const (
	BearerAuthScopes bearerAuthContextKey = "BearerAuth.Scopes"
)

// Defines values for CartItemIssue.
const (
	InsufficientStock CartItemIssue = "insufficientStock"
	NotFound          CartItemIssue = "notFound"
	OutOfStock        CartItemIssue = "outOfStock"
	PriceDecreased    CartItemIssue = "priceDecreased"
	PriceIncreased    CartItemIssue = "priceIncreased"
	Unpublished       CartItemIssue = "unpublished"
)

// Valid indicates whether the value is a known member of the CartItemIssue enum.
func (e CartItemIssue) Valid() bool {
	switch e {
	case InsufficientStock:
		return true
	case NotFound:
		return true
	case OutOfStock:
		return true
	case PriceDecreased:
		return true
	case PriceIncreased:
		return true
	case Unpublished:
		return true
	default:
		return false
	}
}

// CartItemIssue カート明細の再評価結果。取得のたびに商品の現在値と突き合わせて判定される。
// 1 つの明細に複数同時に立ちうるが、notFound は単独で立ち（商品が引けない以上、在庫も価格も
// 判定材料が無い）、outOfStock と insufficientStock は排他（在庫 0 は「不足」ではなく「無い」）。
// いずれの値もカートの取得を失敗にしない。買えない明細があることは要求の不正ではないため。
type CartItemIssue string

// CartItemPutRequest カート明細の数量設定リクエスト（application/json）。対象の商品はパスで指定します。
// 数量は加算ではなく設定です。同じ要求を 2 回送っても結果は変わりません。
// 0 は削除ではなく範囲外として 400 を返します（削除は DELETE が担い、1 つの操作に 2 つの意味を持たせません）。
type CartItemPutRequest struct {
	// Quantity 設定する数量。現在の数量に加算するのではなく、この値で置き換えます。 範囲外の値はドメインに届く前に 400 で拒否されます。
	Quantity int32 `json:"quantity"`
}

// CartItemResponse カート明細 1 件。商品の現在値との突き合わせ結果を issues に添える。
// カートは商品の名前も価格も保持しないため、productName / unitPrice は取得時点の商品の値であり、
// 購入時の金額を拘束しない（請求額を確定するのは購入明細のスナップショット）。
type CartItemResponse struct {
	// AvailableQuantity issues に insufficientStock がある場合の、今買える上限（商品の在庫数）。
	// それ以外の場合は null です。
	AvailableQuantity *int32 `json:"availableQuantity,omitempty"`

	// Issues この明細の再評価結果。空配列なら現時点で購入可能です。
	// subtotalAmount の合算対象になるのは、この配列が空の明細だけです。
	Issues []CartItemIssue `json:"issues"`

	// ProductId 商品 ID。カート内で明細を一意に指す自然キー。
	ProductId openapi_types.UUID `json:"productId"`

	// ProductName 商品名。商品を引けなかった場合（issues に notFound）は null です。
	ProductName *string `json:"productName,omitempty"`

	// Quantity カートに入れた数量。
	Quantity int32 `json:"quantity"`

	// UnitPrice 取得時点の商品の単価。サブセント精度を保持する decimal 文字列（USD ドル）で表します（例 "19.99"）。
	// 商品を引けなかった場合（issues に notFound）は null です。
	// JSON number は IEEE754 double として復元され精度を失うため、文字列で表現します。
	UnitPrice *string `json:"unitPrice,omitempty"`
}

// CartResponse カートのレスポンススキーマ。カート参照・投入・削除の各操作で共有します。
// カートは在庫を押さえず、確定単価も持ちません。買うつもりの控えであって、
// 売り越しの禁止も請求額の確定も購入成立時の関心です。
type CartResponse struct {
	// ExpiresAt カートの有効期限。操作のたびに延長されます。
	// カートがまだ無い場合は行そのものが存在しないため null です
	// （明細を空にしただけの場合はカート行が残るため null になりません）。
	ExpiresAt *time.Time `json:"expiresAt,omitempty"`

	// Items カートの明細。1 件も入っていない場合、およびカートがまだ無い場合は空配列です。
	Items []CartItemResponse `json:"items"`

	// SessionToken ゲストカートのセッショントークン。以降のリクエストで X-Cart-Session ヘッダに載せます。
	// 所有者が確定済みのカート、および新規発行が起きなかった操作では null です。
	// 値が入るのは、その操作がゲストカートを新しく作った場合だけです
	// （取得はカート行を作らないため、常に null になります）。
	SessionToken *string `json:"sessionToken,omitempty"`

	// SubtotalAmount 小計。issues が空の明細（現時点で購入可能なもの）だけを単価×数量で合算した参考値で、
	// USD セント単位の整数です。請求額ではありません。
	// 合算は decimal のまま行い、最後に一度だけ最小単位へ丸めます。
	// 合算対象が 1 件も無い場合は 0 です。
	SubtotalAmount int64 `json:"subtotalAmount"`
}

// ErrorResponse エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ErrorResponse struct {
	// Code 機械的に処理可能なエラーコード
	Code string `json:"code"`

	// Message 人間が読めるエラーメッセージ
	Message string `json:"message"`

	// RequestId リクエストID
	RequestId string `json:"requestId"`
}

// ErrorResponseWithDetails 詳細識別子付きエラーレスポンススキーマ。 この schema を error レスポンスに宣言したエンドポイントだけが details を返せる（opt-in）。 未宣言のエンドポイントでは errorhandler が details を fail-closed で落とす。
type ErrorResponseWithDetails struct {
	// Code 機械的に処理可能なエラーコード
	Code string `json:"code"`

	// Details エラー詳細を示す公開可能な識別子(任意)。検証エラーでは不正なフィールド名が入る
	Details *[]string `json:"details,omitempty"`

	// Message 人間が読めるエラーメッセージ
	Message string `json:"message"`

	// RequestId リクエストID
	RequestId string `json:"requestId"`
}

// CartSessionParam defines model for CartSessionParam.
type CartSessionParam = string

// ProductIdParam defines model for ProductIdParam.
type ProductIdParam = openapi_types.UUID

// BadRequest400 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type BadRequest400 = ErrorResponse

// Conflict409 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Conflict409 = ErrorResponse

// InternalServerError500 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type InternalServerError500 = ErrorResponse

// MethodNotAllowed405 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type MethodNotAllowed405 = ErrorResponse

// ServiceUnavailable503 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ServiceUnavailable503 = ErrorResponse

// Unauthorized401 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Unauthorized401 = ErrorResponse

// UnprocessableEntity422 詳細識別子付きエラーレスポンススキーマ。 この schema を error レスポンスに宣言したエンドポイントだけが details を返せる（opt-in）。 未宣言のエンドポイントでは errorhandler が details を fail-closed で落とす。
type UnprocessableEntity422 = ErrorResponseWithDetails

// basicAuthContextKey is the context key for BasicAuth security scheme
type basicAuthContextKey string

// bearerAuthContextKey is the context key for BearerAuth security scheme
type bearerAuthContextKey string

// PutCartsMeItemParams defines parameters for PutCartsMeItem.
type PutCartsMeItemParams struct {
	// XCartSession ゲストカートのセッショントークン。所有者が未確定のカートへ到達する唯一の手段で、
	// 未認証の呼び出し側はこのヘッダで自分のカートを指定する。
	// 認証済みの呼び出し側では無視される（カートは内部 UserID から解決されるため）。
	// 値は 256 ビットを base64url（パディング無し）で表現した 43 文字。
	XCartSession *CartSessionParam `json:"X-Cart-Session,omitempty"`
}

// PutCartsMeItemJSONRequestBody defines body for PutCartsMeItem for application/json ContentType.
type PutCartsMeItemJSONRequestBody = CartItemPutRequest
