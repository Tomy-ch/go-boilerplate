// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: realtime_system_cqrs.gen.sql

package gen

import (
	"context"
)

const allocateStreamSequence = `-- name: AllocateStreamSequence :one
INSERT INTO realtime_stream_sequences (
    stream_id,
    last_sequence
) VALUES (
    $1, 1
)
ON CONFLICT (stream_id) DO UPDATE
    SET last_sequence = realtime_stream_sequences.last_sequence + 1
RETURNING last_sequence
`

// === source: database/dml/system_cqrs/realtime/allocate_stream_sequence.sql ===
// 業務 tx 内でストリームの次の位置を採番する。行が無ければ 1 から始める。
// 同一ストリームの採番は行ロックで直列化される（ADR-0072）。
//
//	INSERT INTO realtime_stream_sequences (
//	    stream_id,
//	    last_sequence
//	) VALUES (
//	    $1, 1
//	)
//	ON CONFLICT (stream_id) DO UPDATE
//	    SET last_sequence = realtime_stream_sequences.last_sequence + 1
//	RETURNING last_sequence
func (q *Queries) AllocateStreamSequence(ctx context.Context, streamID string) (int64, error) {
	row := q.db.QueryRow(ctx, allocateStreamSequence, streamID)
	var last_sequence int64
	err := row.Scan(&last_sequence)
	return last_sequence, err
}

const currentStreamSequence = `-- name: CurrentStreamSequence :one
SELECT last_sequence
FROM realtime_stream_sequences
WHERE stream_id = $1
`

// === source: database/dml/system_cqrs/realtime/current_stream_sequence.sql ===
// ストリームの現在位置を返す（History の cursor 用）。未採番のストリームは 0 行を返す。
//
//	SELECT last_sequence
//	FROM realtime_stream_sequences
//	WHERE stream_id = $1
func (q *Queries) CurrentStreamSequence(ctx context.Context, streamID string) (int64, error) {
	row := q.db.QueryRow(ctx, currentStreamSequence, streamID)
	var last_sequence int64
	err := row.Scan(&last_sequence)
	return last_sequence, err
}
