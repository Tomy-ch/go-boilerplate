# clock パッケージ

概要: このパッケージは、時刻の取得をはじめとするクロック関連の機能を提供します。

`Clock` を実装し、現在の時刻を取得します。

## 提供する主な機能

- `Clock` 関数: 現在の時刻を取得します。

## 使い方

環境ごとやサービスごとに適切な `Clock` を実装し、アプリケーションのクロック機能を確保します。

システムへの取り込みは、`internal/di/module/infrastructure.go` の `system` に実装を追加してください。
