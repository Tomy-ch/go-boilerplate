> このファイルは `SKILL.md`（canonical / 英語）の日本語参考訳です。スキルとしては読み込まれません（参考用）。

# Impl Issue

GitHub issue を、環境確保からマージ済み PR まで進める半自動パイプライン。進行・記録・検出は機械が持ち、判断はすべて人間が持つ。長い自律実行がブラックボックスにならないのは、承認した計画から外れた箇所が最後ではなくその場で表に出るからである。

コマンドをここに置いているのは、このファイルだけで実行を再現できるようにするため。自ら動く詳細だけをポインタに残す: `.makefiles/README.md`（target 一覧）、`docs/maintenance/db-worktree-pool.md`（スロットプール）、`docs/development-flow.md`（変更種別ごとのフロー）。

## 使うとき

- issue の URL / 番号を渡され、マージ済み PR まで持っていってほしいとき。
- 決定点で止まった実行の再開を頼まれたとき。

以下には使わない: issue の無い変更（`commit` + `submit-pr` を直接）、既存 diff のレビュー（`impl-review` / `test-review` / `settle-comments`）、スキルの作成・編集（`manage-skill`）。

## 契約

| | |
| --- | --- |
| **所管** | issue → merged PR のパイプライン進行、承認済み計画と実物の突き合わせ、人間判断が要る瞬間の機械的検出 |
| **しないこと** | 未決の設計を独自に補完する / 実装判断そのもの（委譲先が持つ） |
| **開始条件** | 採択済みの issue が提示されたとき |
| **停止条件** | 下表の 5 箇所だけ。それ以外では停止せず、判断は PR コメントへ記録する |

## 停止する箇所 — 完全な一覧

このパイプラインがどこで止まるかは仕様であって判断ではない。止まるのは以下だけで、他にはない:

| # | 箇所 | 決めること |
| --- | --- | --- |
| 1 | Step 0 | 4 つのモード。何より先に、連続する 2 回の呼び出しで |
| 2 | Step 3 | 書かれた計画の承認 |
| 3 | Step 4 | 行に halt と書かれた trip-wire |
| 4 | Step 7 | 2 つの peer レビュースキルのうちどれを回すか。それぞれの見込みリターンを添えて |
| 5 | Step 8 | runtime 検証が失敗したとき。およびマージそのもの |

**停止とは、ターンを終えることであって、問うことではない。** 「ユーザーに何かを聞くこと」と読むと、この一覧は破りながら満たせてしまう。実際に起きる失敗は何も聞かないからである。フェーズが片付き、進捗報告を書くのが自然な流れになり、その報告のままターンが終わる — 承認は求めていないので下の禁止は発火しないのに、実行は終わり、残りの作業はユーザーの手に戻っている。一覧は問いにではなく、ターンの終わりに掛けること。

長い実行が読めるものであり続けるのは報告のおかげであり、失敗はそれをターンの最後に置くことのほうである。書いたら、同じターンの中で作業を続けること。「続けます」「次は〜します」は計画ではなくこのバグの証拠である — 次の一手を既に知っているから書けるのであり、それは何にも塞がれていないという意味だからである。告知せずにその一手を実行すること。

**塞がれていることは、停止ではない。** 実行が自分の外側へ渡した先 — CI、バックグラウンドのコマンド、委譲したエージェント — を待っていて、その受け渡しに依存しない作業が何も残っていないなら、そこでもターンは終わる。そこでは誰にも何も決めさせておらず、実行を再開させるのは答えではなく通知である。マージの手順がまさにこれの上に立っており、foreground のループではなくバックグラウンドのコマンドで待てと言っているのはそのためである。判定は、作業についての判断がユーザーの手元にあるかどうかである。あるならそれは停止であり 5 行に属する。無いならその待ちはブロックであり、同じものを待っていない作業が何も残らなくなった時点でだけターンを終える。

停止点に見えて停止点でない瞬間が 3 つある。いずれも一覧に無い停止が忍び込む場所である:

- **フェーズの境目。** Step 3 の承認は Step 4〜9 全体に掛かる。計画が実行全体を列挙しており、承認されたのはそれだからである。フェーズが終わることは出来事ではない — 継ぎ目もそうである。継ぎ目では run 記録を書き、コンパクションを推奨して進む。PR の継ぎ目は先に聞くが、聞いている対象は作業ではない。
- **サブエージェントの完了通知。** レビューや監査は fan-out する。報告が届くのは作業が再開する場所であって、止まる場所ではない。
- **Step 0 で決まったモード。** それは行使済みの権限である。レビューモードが既に認めた修正を確認し直すのは、同じことを二度承認させることにあたる。

1 件ずつ問えば停止は常に安く見え、その費用は拡散している。だから個別の判断では毎回「聞く」が勝つ。上の一覧を助言ではなく閉じた列挙にしてあるのはそのためである。

## このスキルが持たないもの

実装判断を一切持たない。どの設計を採るか、レビュアーが正しいか、その指摘が issue に値するかを決めない。ユーザーへ回して、答えを記録する。

| 作業 | 担当 |
| --- | --- |
| コミット分割と実行 | `commit` |
| push + PR 作成/更新 | `submit-pr` |
| 変更そのもののレビュー | `impl-review` |
| テストのレビュー | `test-review` |
| 触れた宣言のコメント蓄積 | `settle-comments`（Step 4 の末尾で無条件に） |
| 実装そのもの | 承認された計画に従うあなた |

2 つのレビュースキルは peer である。互いを呼び出さず、それぞれ個別に諮る（Step 7）。`settle-comments` はそこに含まれない —— Step 4 の末尾で無条件に走る。

## AI の変更範囲

`AGENTS.md` は AI の編集対象を `internal/` / `pkg/` / `database/` / `openapi/` に限り、それ以外 — `.github/workflows/`、`docker/`、`scripts/`、`docs/`、`.makefiles/`、ルートのドットファイル — をスコープ外としている。**このスキルの起動は、その制限を緩めるユーザーの明示指示にあたる。** このスキルは issue 汎用のドライバであり、対象面を決めるのは issue の側だからである。CI・ツールチェーン・コンテナイメージ・ドキュメントについての issue は、既定の 4 ディレクトリの中では解決できない。

緩和には境界があり、その境界は計画である:

- Step 3 の計画の **触るファイル一覧** が許可された対象面。既定の 4 ディレクトリの外にあるセンシティブなパスは、編集する**前**にそこへ現れていなければならない。glob で暗に含めるのではなく、明示的に名前を書く。
- 計画を提示するときにそれを言う。ユーザーが diff で気づくのではなく、承知のうえでセンシティブなパスを承認できるように。
- 計画に無いセンシティブなパスへ到達したら、flow モードに関わらず停止する（Step 4 の trip-wire 1′）。聞くこと。対象面を広げてから事後報告しない。

このスキルの実行中も保護されるもの（issue が何を要求しても触らない）:

- `AGENTS.md` / `CLAUDE.md`
- 生成物: `**/*.gen.go`、`*.sql.go`、`*_mock.go`、`**/openapi.gen.yaml`、および `docs/` 配下の生成物（`docs/openapi/**`、`docs/coverage/**`、`docs/db-schema/**`、`docs/godoc/**`、`docs/portal/docs.json`、`docs/portal/guides/**`）。`make` ターゲット経由での再生成はよい。手編集は駄目。
- `.claude/settings.json` の `permissions.deny` 配下
- `database/migrations/**` の既存ファイル（新規 migration ファイルのみ）

## Step 0 — 4 つのモードを確認する（連続する `AskUserQuestion` 2 回）

何より先に、連続する 2 回の呼び出しで聞く。実行方針の 3 モードと、計画モードである。2 回に分けるのは、両者が別のことを聞いているから — 実行がどう振る舞うか、と、計画フェーズがいくら払うか — であり、また 1 回の呼び出しは 4 問で頭打ちで、そのままでは 5 つ目のモードを足す余地が永久に無いからである。**これは依然として停止点 1 つである。** 実行は 2 回の間に何もしない。2 枚目のダイアログは 2 つ目の停止ではない。

既定は明示するが、ユーザーの選択が常に優先。

### 1 回目 — 実行方針

**レビューモード** — レビュー指摘をどうするか。

| モード | 確定した指摘 | それ以外 |
| --- | --- | --- |
| `all` | 大きく書き換わっても適用する | — |
| `harmful`（既定） | 変更範囲内で明確に有害なものだけ適用 | issue モードへ回す |
| `issues` | 何も適用しない | issue モードへ回す |

**issue モード** — 無関係な指摘をどう追跡に載せるか。

| モード | 挙動 |
| --- | --- |
| `search`（既定） | 既存 issue を先に検索し、重複ならそこへコメント |
| `file` | 検索せず起票 |

`issues` × `file` は起票数が最大になる組み合わせ。実行前に件数を出して確認する — レビューは容易に十数件出るし、十数件の新規 issue はそれ自体がノイズ。

**進行モード** — trip-wire に当たったときどうするか。名前に引き金を含めてあるのは、このモードが trip-wire だけを支配するからである。実行全体の姿勢ではなく、他の 4 つの停止点には一切届かない。

| モード | 挙動 |
| --- | --- |
| `record-on-tripwire`（既定） | 判断を記録して続行し、最後に PR コメント 1 本へまとめて出す |
| `halt-on-tripwire` | その trip-wire で停止して聞く |

**どちらのモードも、Step 4 で halt と記された trip-wire には届かない。** それらはアーキテクチャ・ドメイン・ポリシーの判断であり、`AGENTS.md` が無条件に人間のゲートを置いている。モードが決めるのは残りの行の扱いだけ: `record-on-tripwire` は run 記録へ追記して続行し、`halt-on-tripwire` はそこも聞く — ユーザーが在席していて、スコープの伸びを最後ではなくその場で見たいときに選ぶ価値がある。

### 2 回目 — 計画モード

**計画モード** — 計画フェーズにいくら払うか。Step 3 は 3 段あり、そのどれを走らせるかをこれが決める。**どのモードでも Step 3 の不変条件（計画は実装者と異なるモデルの目を通る）は満たされる** — ここで手放しているのはそれではない。変わるのは計画が何パス受けるか、そのうち何本が上位層に載るかである。

| モード | 走る段 | コスト |
| --- | --- | --- |
| `full`（既定） | 3a + 3b + 3c | 3 パス — 起草の 1 本に加えて上位層が 2 本 |
| `draft-review` | 3b + 3c | 2 パス。観点整理を省く |
| `single` | 3b のみ。実装者と異なるモデルが起草する | 1 パス |

**聞くときは、何をするかだけでなくいくら掛かるかを言うこと。** これは issue の大小に関わらず毎回支払われる唯一のモードなので、値段を言われない既定は、事故として支払われる既定である。1 行のドキュメント修正と層をまたぐ機能追加が同じ計画予算を買う理由は無く、そしてどちらであるかは、Step 3 が何かを読む前の時点ではユーザーしか知らない。

issue が層をまたぐ・契約を変える・設計判断を名指ししているなら `full`、形は既に決まっていて詰めるだけなら `draft-review`、計画が形式にすぎない程度の変更なら `single` を薦める。3 つを値段抜きで並べるのではなく、どれを薦めるかとその理由を言うこと。

## run 記録 — 「判断を記録する」の書き先

このパイプラインは記録することを命じられている。続行した trip-wire、飛ばしたゲート、却下したレビュー指摘、ユーザーへ預けた判断。Step 9 はそれらすべてを最後の PR コメントへ出す義務を負う。

**書き先はファイルであり、起きた時点で追記する** — 計画ファイルと並べて、gitignore された `tmp/` の下に、issue の名前で置く。会話へ書くのではない。

長い実行は自分のコンテキストより長生きする。覚えているだけのものは途中のどこかで要約に吸われ、しかもその失敗は両方向に無音である: Step 9 は変わらず自信のある PR コメントを書き、そこには項目が欠けたことを示す一文も無い。ファイルであることは再開可能性そのものでもある。後から引き継ぐセッションは、決着したモードとそれ以降に発火したものを取り戻せる — worktree をいくら観測しても分からないことである。

1 イベント 1 行で追記する。各行は何が起きたかと、何を決めたかを持つ。

| 書く場所 | 項目 |
| --- | --- |
| Step 0 | 決着した 4 モード |
| Step 3 | 計画ファイルのパスと、3a / 3c を走らせたか |
| Step 4 | 発火した trip-wire すべて — 番号、何が引いたか、下した判断 |
| Step 6 | 走らせなかったゲートすべてと、その理由 |
| Step 7 | 却下した指摘、または提案と別の直し方をした指摘すべてと、その理由 |
| Step 8 | runtime 検証が覆ったものと、覆わなかったもの |

まとめて最後に書くのではなく、起きた時点で書く — まとめ書きこそコンパクションが食うものである。Step 9 はこのファイルを読んでコメントを組み立てる。実行を思い出して組み立てるのではない。

## 継ぎ目 — 畳んで安い場所

実行のうち 2 点は、先へ持ち越すものがほとんど無い。必要なものが既にディスク上にあるからである。名前を付けておく価値があるのは、放っておいたコンパクションは窓が埋まったところで起きる — つまり日常的に実装の途中で起きる — からである。

| 継ぎ目 | 以降が必要とするもの | それが既にある場所 |
| --- | --- | --- |
| Step 5 の後（実装を突合し終えた時点） | 承認済み計画、差分、ここまでの判断 | 計画ファイル、`git diff`、run 記録 |
| PR を開いた後（Step 8、runtime 検証の前・CI が走っている間） | PR、ブランチ、ここまでの判断 | GitHub、`git`、run 記録 |

どちらの継ぎ目でも、**まず run 記録を書き、次にコンパクションを推奨し、そして進む。** 触れるだけでなく推奨する — 誰も動かない選択肢を告知するだけの継ぎ目は何も節約しないし、記録が既に以降を復元可能にしている以上、ここでコンテキストを抱える理由が無い。

**PR の継ぎ目では聞く。** その先にあるもの — `make serve`、curl の記録、トレース、CI ログ — は実行に残る中で最も重い読み物であり、だからここはコンパクションが最も効き、読まれない告知が最も高くつく場所である。例外は 1 つだけ、全権委任のもとでユーザーが離席している実行である。そこでは告知して続行する。答える人が居ない質問は、自力で完遂しろと言われたまさにその瞬間に実行を止めるからである。

Step 5 の継ぎ目は聞かずに推奨する。実行が先へ進める仕事の手前にあるので、ここで止めても PR の継ぎ目が既により良く買っているもの以上は買えない。

**どちらの継ぎ目も停止点ではなく、一覧は 5 箇所のままである。** あの一覧が数えているのは**作業について**の判断をユーザーが持つ場所であり、コンパクションは作業について何も決めず、実行はどちらに転んでも進む。ただし PR の継ぎ目で聞けば、答えが返るまで進行は止まる。無人の実行では、その停止を無料と見なさずそう言うこと。

実装フェーズは subagent へ委譲できない唯一のフェーズであり、だから窓が実際に埋まるのはそこである。計画の各段もレビューも既に subagent として走り、その窓はこちらへ届かない。継ぎ目がこの位置にあるのはその非対称性による。

## Step 1 — 起動

```bash
printf '\033]0;%s\007' "<issue-number>-<slug>"   # 並行実行を見分けられるよう窓に印を付ける
gh issue view <n> --json number,title,body,labels,state,comments
```

**何か書く前に、issue 本文をベース実物と突き合わせる。** issue 本文は書かれた時点のスナップショットで、行番号・「X はまだ無い」・「Y に consumer が無い」は陳腐化する。これから切るベースに対して、本文の事実主張を 1 つずつ検証する。

その上で着手コメントを投稿する。ブランチ名・ベース commit・隔離方式、そして何より **上で見つけた食い違いすべて** を書く。issue を「着手済み」とマークすると同時に、まだ安いうちに訂正をユーザーへ渡すため。

```bash
gh issue comment <n> --body-file <file>
```

## Step 2 — 実行環境を確保する

コードに触れる前に済ませる。共有 checkout に何も落とさないため。どちらの半分を実行するかは worktree が既にあるかで決まる。新規に用意することと既存へ再開することは別の操作で、動いている worktree に前者を当てると作業が壊れる。

### 初期セットアップ — worktree がまだ無い

```bash
# 1. 現行のリリース線を origin の実状態から解決する。
BASE=$(make -s base-branch)
test -n "$BASE" || { echo "ベースブランチを解決できませんでした"; exit 1; }

# 2. 古いローカル ref ではなく、今の origin から切る。
git fetch origin "$BASE"
git worktree add -b feature/<n>-<slug> ../go-boilerplate.worktrees/<n>-<slug> "origin/$BASE"

# 3. DB スロットをリース: 専用 DB（wt<N>_local / wt<N>_test）、API ポート 8080+N、mock-auth 2010+N。
cd ../go-boilerplate.worktrees/<n>-<slug> && make slot-acquire

# 4. 新規 worktree には vendor/ が無く、air は --mod=vendor でビルドするため、これが無いと serve が失敗する。
go mod vendor
```

`make base-branch` は `origin` の実状態を読む。これ以外を使わないこと。ローカルの `refs/remotes/origin/HEAD` は clone 時に一度決まったきり `git fetch` では更新されず、GitHub のデフォルトブランチは前のリリースラインに留まり、ハーネスが提示する "Main branch" はその陳腐化したローカル symref を読んでいる。3 つとも警告なく答えるため、1 世代前のベースから切ったことは、サブエージェントが「あるはずのファイルが無い」と報告するまで表に出ない。そこまで進んだ作業は丸ごと無駄になる。

`slot-acquire` が失敗を報告したら、再試行の前に `make slot-status` を見る — コマンドがエラーでもリースだけ成功していることが多い。

**スロットは自分から解放しない。片付けフェーズで解放を聞きもしない。** 握ったままのコストはほぼゼロ（リースは stale になれば自動回収される）だが、作業途中で失うと DB とポートを失う。本当に終わったかを知っているのはユーザーだけ。

ユーザーの指示が解決結果と違うリリースバージョンを指していた場合は、切る前に聞く。意図的な backport 先だけは解決器に知りようがない。

### 既存 worktree へ再開する

セットアップは済んでいる。観測して結果を報告するだけにし、どれ一つ再実行しない。

**何よりも先に run 記録を読む。** worktree を観測して分かるのは作業がどこにあるかであって、それについて何が決まったかではない — 決着したモード、発火した trip-wire、飛ばしたゲートはあのファイルにしか無い。読み飛ばした再開は、判断の履歴が白紙の状態から黙って走り直し、Step 9 のコメントからそのすべてを失う。

session-start の環境行（`[agent-env] checkout=… branch=… vendor=… db-slot=…`）が、重要な 3 つの事実 — どの checkout か、`vendor/` があるか、どの DB スロットを保持しているか — を既に運んでいる。存在するなら、導出し直さずそのまま報告してから先へ進む。

ただしそれがある前提に寄りかからない。hook は無いことも、無効化されていることも、そもそも実行しないハーネスに繋がっていることもある。その場合のフォールバックは修復ではなく確認である。以下はいずれも読むだけ:

```bash
git rev-parse --show-toplevel
test -d vendor && echo 'vendor: present' || echo 'vendor: absent'
cat .gobp-db-slot 2>/dev/null || echo 'slot: none'
```

スロットが無い・壊れているのは報告すべき事実であって、その場で直すべき障害ではない。`make slot-acquire` は DB 作業が実際に始まる直前 — 最初の `make go-test` / `make serve` / `psql` — に実行し、それより前には実行しない。`go mod vendor` も同じで、`vendor/` が無く、かつビルドが目前に迫っているときに走らせる。再開の儀式として走らせるものではない。

**セッションを再開したというだけの理由でスロットを取得したり DB を再初期化したりしない。** `slot-acquire` と `db-*-reinit` はスロットの `wt<N>_local` / `wt<N>_test` を作り直すため、反射的な再開時取得は、まさに再開しようとしている実行の状態 — 投入済みのシード、途中まで当てた migration、障害の診断に使うはずだったデータ — を破壊する。

## Step 3 — 計画を立て、そこで待つ

**不変条件: 計画は、実装者と異なるモデルの目を最低 1 回通る。** 以下の 3 段はそれを満たす既定の実現形であって、規則そのものではない。規則はここに書かれたモデル名からではなく、そのセッション自身のモデルから読むこと。

後段のどのゲートも計画を開き直さない — `impl-review` / `test-review` / `settle-comments` はいずれも出来上がった変更を subject にするので、「計画がそもそも issue を解いているか」はここで見るか、どこでも見ないかのどちらかである。うまく書くことと偏らずに書くことは別の仕事なので、段を分ける。**3 段で停止点は増えない** — 最後の承認は 1 回だけの待ちである。

| 段 | 実行モデル | 出力 | 走るモード |
| --- | --- | --- | --- |
| 3a 観点整理 | 実装者とは別系列の、上位に近い層 | 計画が答えるべき問いだけ。それ以外は出させない | `full` |
| 3b 情報収集・起草 | 調査と起草に使える最上位層を subagent として | 計画ファイル | 全モード |
| 3c 計画レビュー | 3a と同じモデル。`single` では 3b が不変条件を担う | 指摘。計画ファイルへ独立した節として追記する | `full` / `draft-review` |

**各段のモデルは実行時に解決すること。このファイルから読まない。** モデル ID は `<系列><世代>` の形で、セッションはどのモデルが動いているかを示すので、この節が必要とする 2 つの事実 — どの系列が実装者のものか、どの層がどの層の上か — は Step 3 の実行時に揃っている。スキルに書かれた名前は変わり続ける一覧のスナップショットにすぎないが、上の性質は変わらない。`single` では 3b のモデルが実装者と別系列であること。計画が受ける唯一のパスがそこになるためである。

### 3a — 観点整理

issue 本文、Step 1 で見つけた issue ↔ ベースの食い違い、既に読んだパスを渡す。

**返させるのは答えではなく問いである。** この時点でリポジトリをほとんど読んでいない以上、断定させれば出てくるのは一般論であり、その一般論をより強い起草者へ渡すと、視野を広げるどころかアンカーとして働く。「何を決める必要があるか」「この形の計画が普段どこを落とすか」を聞くこと。計画案・推奨・方向性が返ってきたら受け取らない。

### 3b — 情報収集・起草

subagent として実行する。オーケストレータが溜め込んだ枠組みを持ち込まない窓で調査させるためである。issue、Step 1 の訂正、既に読んだパス、そして 3a の問いを渡す。「私の要約を鵜呑みにせず自分で検証しろ」と伝える。

3a の問い **すべて** に答える義務を負う — どう決めたか、あるいはこの件には該当しないか、のいずれかで。

計画はチャットの発言ではなく**ファイル**にする。Step 5 が機械的に突合するため。repo の gitignore された `tmp/` 配下に置く（運用者の好みで repo 外ディレクトリへの symlink でもよい）。次のセクションが必須:

| セクション | なぜ必須か |
| --- | --- |
| 触るファイル一覧 | Step 5 が `git diff --name-only` と突合する |
| 各ステップの成果物 | 途中で止まった実行の再開・引き継ぎができる |
| 確定した選択肢と**棄却した選択肢（理由付き）** | 棄却した案を後から採ったとき trip-wire 2 が発火する |
| ゲート表 | runtime 検証の要否を計画時に固定し、後から黙って落とせなくする |

### 3c — 計画レビュー

要否は決め打ちせず導出する:

- **3b のモデルが実装者と同じ** — 通常のケースで、起草に使える最上位層がこのセッションを動かしているモデルでもある場合。計画はまだ他のどのモデルの目も通っていないので、**3c は必須**。
- **3b のモデルが既に実装者と異なる** — セッションが最強の起草者ではない系列で動いている場合。3b が終わった時点で不変条件は満たされており、3c は任意 — 二度目を通す価値がある規模なら回し、無ければ省く。

指摘は計画ファイルへ独立した節として追記し、**計画に溶かし込まず、計画と並べて提示する** — 計画を黙って書き換えるレビューアは、ユーザーが承認を求められているその瞬間に不一致を隠すことになる。各指摘は下の承認の場でユーザーが裁定する。

### そして待つ

計画を提示し、**承認を待つ。承認前に実装しない。**

**その承認は Step 4〜9 に掛かる。** 計画が実行全体を列挙している以上、その内側のフェーズを承認し直す必要はない。実行は次の停止点まで自力で進む。

## Step 4 — 実装し、5 つの trip-wire を監視する

計画が承認され、実装が始まる。決める時間と作る時間の境界であり、それを知っているのはこのスキルだけ
である。

```sh
.agents/closed-loop/marks.sh planApprovedAt 2>/dev/null || true
.agents/closed-loop/marks.sh implStartedAt 2>/dev/null || true
```

承認された計画に従う。以下は意図的に機械的なトリガーにしてある — 「判断が重要だったと**気づく**」ことに依存させるのが、まさに drift が報告されない原因だから。

| # | trip-wire | 既定 | なぜ |
| --- | --- | --- | --- |
| 1 | 計画に無いファイルを触った（既定 4 ディレクトリの**内**） | 記録 | スコープは伸びたが、`AGENTS.md` が既に許している面の中 |
| 1′ | 同上（既定 4 ディレクトリの**外**。`docker/` / `scripts/` / `.github/` / `docs/` / `.makefiles/` / ルートのドットファイル） | **停止** | 計画が許可された対象面。広げるかはユーザーの判断 |
| 2 | 計画が棄却した案、または第三の案を採った | **停止** | 棄却には理由があった。黙って覆すとその理由が捨てられる |
| 3 | アーキ規約由来の lint/CI 失敗（`interfacebloat` / `gocognit` / `depguard` / architest ほか） | **停止** | これらは書式ではない。満たすには設計が変わる |
| 4 | レビュー指摘を却下した、または提案と別の修正を採った | **停止** | 指摘が正しくても提案された修正が有害なことがある。単独で決める判断ではない |
| 5 | ゲートを飛ばした | 記録 | Step 6 が既に PR への明記を要求している |

**コメントのパスを走らせるまで実装は終わっていない。** コードは裸で書き、そのあとでこの変更が触れた宣言に対して `settle-comments` を起動する。`AGENTS.md` がこれを Step 7 ではなくここへ置いているのは、生成が止まったあとでしか判定が効かないためであり、**無条件**である —— 見込みリターンを見積もらず、回すかどうかもユーザーに尋ねない。スキル自身の質問は依然として要るので、スコープ（触れた宣言）と適用モードを渡し、この実行が既に決着させたことを再び問わせない。

**停止の行は、どちらの進行モードでも停止する** — アーキテクチャ・ドメイン・ポリシーの判断に `AGENTS.md` が置いている人間のゲートだからである。発火したら状況と推奨を提示する。`halt-on-tripwire` は記録の行にも同じ扱いを広げ、`record-on-tripwire` はそれらを記録して続行し、Step 9 が記録済みの判断を PR コメント 1 本へまとめる。

### コード生成が塞がれているとき

生成系 make target は `docker compose run … make <target>-ci` の薄いラッパで、`-ci` 側は mise が入れたツールで host 実行する前提で書かれている。コンテナランタイムが使えないときは直接叩く:

```bash
make merge-dml-ci work-dir="."     # DML 連結
make sqlc-generate-ci              # sqlc
make gen-bundle-oapi-ci            # OpenAPI bundle
make gen-api-docs-ci               # docs/openapi/index.html
cd <pkg> && mockgen -source=<f>.go -destination=mock/mock_<f>.go.gen.go -package=mock_<pkg>
```

本当にコンテナが要るのは `make dump-schema` だけで、しかも migration を足したときだけ。

罠が 2 つ。`merge-dml-ci` は `go run ./cmd/` を走らせるため、クエリ生成前に Repository メソッドを足すとビルドが詰まる — 生成の間だけ実装をスタブにし、後で戻す（先に `cp` で退避）。もう 1 つ、埋め込み spec の `//go:generate` はコンテナパスを指しているので `go generate` では回らない。実パスで直接叩く:

```bash
cd internal/controller/httpstack/oapi/validator \
  && oapi-codegen --package=gen --generate=spec -o ./gen/validate.gen.go <repo>/openapi/openapi.gen.yaml
```

OpenAPI の description を 1 行変えるだけでも生成物は 3 つ動く: bundle、`docs/openapi/index.html`、その埋め込み spec。1 つでも漏らすと CI の生成物チェックが落ちる。

## Step 5 — 計画と実物を突合する

ゲートの前に行う。突合するのは:

- `git diff --name-only` ↔ 計画のファイル一覧（増えた分・触られなかった分の両方を報告）
- 実際に採った選択肢 ↔ 計画の確定/棄却リスト
- ゲート表 ↔ 実際に走らせたもの

差分を提示する。長い実行は正当な理由で drift する。問題なのは**ユーザーが見ていない drift**。drift が無ければ 1 行そう言って進む。

## Step 6 — ローカルゲート

`make go-fix` → `make go-lint` / `make go-test`。worktree が多いときは CI へ委ねてよいが、**ローカル未実行であることを PR に書く**。黙っていると「検証済み」と読まれる。

runtime 検証は意図的にここに置いていない。PR ができた後（Step 8）に置くことで、CI と並行して走らせられる。

## Step 7 — レビュー

出来上がった変更には 2 つのレビュー対象があり、それぞれに 1 つのスキルが対応する: `impl-review`（変更そのもの）と `test-review`（テスト）。2 つは peer であり互いを呼び出さないので、このステップが黙って 1 つを選んではならない。コメント蓄積はここでの第 3 の対象では**ない** —— `settle-comments` は実装の一部として Step 4 で既に走っており、見積もるものが残っていない。

`AGENTS.md` の Review Phase Protocol に従う: **この実行が既に持っている文脈から、各スキルの見込みリターンを見積もる** — どの層に触れたか、テストが動いたか、先行するパスが何を見たか — そのうえで見積もりと理由を添えてスキルごとにユーザーへ諮り、承認されたものを回す。「3 つとも回しますか」は質問ではない。費用を値付けせずユーザーへ投げ返している。

見積もりが最も安く作れるのはこのステップである。計画・diff・Step 5 の突き合わせが既に手元にある。

指摘は Step 0 のレビューモードに従って処理する。自動適用してよいのは機械的に判定できるものだけ — 書式、lint 修正、コメント指摘、生成物の再生成。**設計を変える修正は、レビューモード `all` であっても必ず決定点**にする。`all` が許可しているのは「大きな書き換え」であって「未レビューの書き換え」ではない。

その後、記録した trip-wire と保留した判断をまとめて 1 箇所で提示する。小出しより一括のほうが、実行全体の形が見える。

## Step 8 — PR → runtime 検証 → マージ

このステップが PR をマージしたら打刻する。ここで行うマージは、ループが後から `gh` を引くまで
他の誰も観測していない。

```sh
.agents/closed-loop/marks.sh mergedAt 2>/dev/null || true
```

先に `submit-pr` で PR を開く。ローカル検証の間に CI が回り始める。

### runtime 検証 — マージのゲート

動いているシステムに対して実 HTTP 経路を通す。どのモードでも緩められない。

```bash
make serve                                    # API は 8080+N、mock-auth は 2010+N

TOKEN=$(curl -fsS -X POST http://localhost:201N/default/token \
  -d 'grant_type=password' \
  -d 'client_id=go-boilerplate-client' \
  -d 'password=unused' \
  --data-urlencode 'username=<seeded-subject>' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')

curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer $TOKEN" http://localhost:808N/v1/...
```

この手順の正本は `docs/design/auth.md` —— mock provider の標準トークンエンドポイントで、ブラウザを介さずにトークンを発行する。この抜粋と同文書が食い違うときは、文書の側が正しい。

token の subject は seed が登録した identity の `subject` 文字列でなければならない。内部の UUID ではない — シードの UUID 行はスロットのポートが発行する issuer とは別の issuer に属するため、UUID を渡すと紛らわしい 401 になる。実在する subject は DB から引く:

```bash
docker exec gobp-shared-database-1 psql -U postgres -d wt<N>_local -c \
  "select subject from <identity テーブル> where issuer = 'http://localhost:201N/default';"
```

正常系、変更が持ち込むエラー経路、そして保護された操作ならトークン無しで 401 になることを確認する。その上で **LGTM スタックのトレースを読み、リクエストが想定どおりの経路（controller → usecase → infrastructure、意図した SQL）を通ったことを確認する**。ステータスコードだけでは、そのリクエストが変更した層に届いたことの証明にならない — 間違ったが尤もらしい経路は、間違った理由で正しいステータスを返す。

**CI 緑はこの代替にならない。** レビューレンズも CI チェックも、DB 層で止まる静的解析やテストにすぎない。middleware がそもそもリクエストを届かせない層で、記載されたステータスコードが正しいとされてしまう。実 HTTP リクエスト 1 本で即座に決着する。

runtime 検証がどうしても実行できないときの正直な選択肢は 2 つで、第三はない。

1. まだマージしない。
2. 同じ HTTP 経路を通す統合テストを足し、それを根拠にマージする。

どちらを採ったかを明言する。

### マージ

foreground の sleep ループでセッションを潰さずに CI を待つ。チェックが確定したら終了する background コマンドを使い、通知を 1 回だけ受ける:

```bash
until [ "$(gh pr checks <n> --json bucket --jq '[.[]|select(.bucket=="pending")]|length')" = "0" ]; do sleep 30; done
gh pr checks <n>
```

その上で、マージの可否を聞くこと。停止点の表の 5 行目はマージそのものをユーザーの手に置いている。緑のチェックはその問いの答えではなく前提である。CI が何と言い、runtime 検証が何を見つけたかを報告し、承認を得てから `gh pr merge <n> --merge` を実行する。

## Step 9 — 締め

issue は**手動で**クローズする。PR のベースが default branch ではなくリリースブランチのとき、自動クローズのキーワードは発火しない:

```bash
gh issue comment <n> --body-file <handover> && gh issue close <n>
```

申し送りコメントには、決めたこと・計画から想定外だったこと・意図的に手を付けなかったことを書く。

変更範囲の外にある指摘は issue モードに従って追跡へ回す。`search` では、新規起票より既存 issue への申し送りコメントを優先する — issue 数それ自体がコストで、重複は元の issue を埋もれさせる。

**起票の前に、その指摘を動いているシステムで検証する。** コードを読んだだけで導いた指摘は、静的レビューでは捕まえられない形で誤りうる — 多くの場合、読んでいた層の外側（middleware、DI 配線、DB）が既にそのケースを処理しているためである。Step 8 の runtime 段階があれば大抵は確認できる。

最後に、コミットメッセージにも PR 本文にも現れていない自己判断を PR コメントへ記録する。**run 記録を読み、上から順に落としていく。** 停止せず記録した trip-wire はすべてここに落ちるが、それらが揃って残っている場所はあのファイルだけである。

## 二重に聞かせない委譲

サブスキルはそれぞれ自前の質問を持つ。このスキルが既にユーザーと決着させている以上、答えを payload で渡してサブスキル側のゲートを飛ばす。

| サブスキル | 渡すもの | 抑止されるもの |
| --- | --- | --- |
| `commit` | 既に提示済みのコミット分割 | 分割の承認質問 |
| `submit-pr` | レビュー実行済みであること、push の判断 | Phase 0 のレビュー確認と push 確認 |
| `impl-review` | スコープ、レビュアーモデル | Step 0 |
| `test-review` | スコープ、レビュアーモデル | スコープの質問 |
| `settle-comments` | スコープ**と適用モード** | スコープと適用モードの質問 |

**どの行も必須である。1 行欠けると、このスキルが既に決着させたゲートが復活するからだ。** 既定が 1 件ずつ確認であるサブスキル — 見るべきは `settle-comments` — は、適用モードが届かなければまさにそう振る舞う。しかも欠落は、質問が始まるまで見えない。

同じことを 2 回聞くと、ユーザーは読まずに承認する癖がつく。それはこのスキルが作ろうとしている決定点そのものを無効化する。

## やること / やらないこと

- ✅ コードに触れる前に worktree とスロットを確保する。
- ✅ 再開時は worktree を観測し、見えたもの — パス・`vendor/`・スロット — を報告する。
- ✅ issue の主張をベース実物で検証し、食い違いを着手コメントに書く。
- ✅ 実装前に計画の承認を取り、Step 5 が突合できるようファイルとして残す。
- ✅ 計画を、実装者と異なるモデルの目に通す。そのレビューの指摘は計画に溶かし込まず並べて提示する。
- ✅ 5 つの trip-wire を「気づくもの」ではなく機械的トリガーとして扱う。
- ✅ 一覧にある 5 箇所でだけ停止し — 停止とはターンを終えることであって、聞くことだけではない —
      それ以外の判断は起きた時点で run 記録へ追記する。
- ✅ 継ぎ目では run 記録を書き、コンパクションを推奨する — PR の継ぎ目では聞き、Step 5 の継ぎ目では告知し、全権委任で離席している実行ではどちらも告知にとどめる。
- ✅ サブスキルには決着済みの答えを、適用モードも含めて渡す。
- ✅ どのゲートを走らせ、どれを走らせなかったかを明言する。
- ✅ ステータスコードだけでなくトレースを読む。
- ✅ issue を起票する前に runtime で指摘を検証する。
- ❌ HTTP 経路を一度も通していない実装コードの変更をマージする。
- ❌ CI 緑を runtime 検証として提示する。
- ❌ 設計を変える修正を、どのモードであれ自動適用する。
- ❌ フェーズの境目で承認を求める、またはサブエージェントの完了を境目として扱う。
- ❌ 進捗報告でターンを終える。報告は書いて、同じターンの中で続けること。
- ❌ 観点整理の段に、問いではなく計画案・推奨・方向性を返させる。
- ❌ どのレビュースキルを回すかを自分で選ぶ、または片方が連鎖する前提で一方だけ回す。
- ❌ issue モードが指示しない限り、既存を確認せず起票する。
- ❌ 頼まれてもいないのに DB スロットを解放する、または解放を聞く。
- ❌ セッションを再開したというだけの理由でスロットを取得する、または DB を再初期化する。
- ❌ foreground の sleep ループで CI をポーリングする。
- ❌ 実行中の判断をコンテキストだけで持つ、または Step 9 のコメントを思い出しで組み立てる。
- ❌ 継ぎ目を停止点として扱う、またはそこで待つ。

## チェックリスト

- [ ] Step 0 の 2 回の呼び出しで 4 モードを確認し、計画モードはコストを添えて聞いた。
- [ ] 着手コメントを投稿した（issue ↔ ベースの食い違いを含む）。
- [ ] Step 2 の正しい半分で実行環境を確保した: 新規なら fetch 済みのベースから worktree を作成し、スロットをリースし `go mod vendor` を実行 — 既存なら観測して報告し、再開しただけでスロットを取得も DB を再初期化もしていない。
- [ ] 計画モードが選んだ段で計画を作成し、3a を走らせたならその問いすべてに答えがあり、必須 4 セクションが揃い、実装者と異なるモデルの目を通した上で、実装前に承認された。
- [ ] trip-wire を、行の既定と進行モードどおりに処理した。黙って吸収したものが無く、すべての判断を起きた時点で run 記録へ追記した。
- [ ] 継ぎ目を 2 つとも通った。記録を書き、コンパクションを推奨し、無人・全権委任でない限り PR の継ぎ目では聞いた。
- [ ] 一覧の 5 箇所以外で停止しておらず、フェーズの境目でも進捗報告でもターンを終えていない。
- [ ] 計画と実差分を突合した。
- [ ] ローカルゲートを実行した、または CI へ委ねたことを PR に書いた。
- [ ] コメントのパスを Step 4 の末尾で無条件に走らせた。2 つのレビュースキルをそれぞれ見積もってユーザーへ諮り、承認されたものを決着済みの答えを渡して回した。自動適用は機械的に判定できるものに限定した。
- [ ] 決定点をまとめて提示した。
- [ ] PR を開いた上で runtime 検証（curl + トレース）を完了した — または未実施であることと、2 択のどちらを採ったかを明言した — 上でマージした。
- [ ] issue を申し送り付きで手動クローズ。無関係な指摘は issue モードどおりに処理し、各々起票前に検証済み。残った自己判断を PR コメントへ記録した。
