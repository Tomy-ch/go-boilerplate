# コードベース知識グラフ（/graphify）の直接宣言。バージョンを決めるのはこのファイルだけで、
# 推移依存まで含めた解決結果と sha256 は graphify.txt が持つ。更新は `make py-lock`。
#
# sql extra は database/dml/** を解析対象に入れるために要る。
# 0.9.25 は最新ではない。0.9.30 は公開 1 日で PyPI のクールダウン 7 日に満たないため、窓を
# 満たす最新を採っている。
graphifyy[sql]==0.9.25
